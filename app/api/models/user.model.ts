export interface User {
  /**
   * The ID of the user.
   */
  id: string;
  /**
   * The username of the user.
   */
  username: string;
  /**
   * The phone number of the user.
   */
  phoneNumber: string;
}
